import type { Config } from "tailwindcss";

const config: Config = {
  darkMode: "class",
  theme: {
    extend: {
      colors: {
        accent: "#880808",
      },
    },
  },
};

export default config;
