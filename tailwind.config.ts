import type { Config } from "tailwindcss";

const config: Config = {
  darkMode: "class",
  theme: {
    extend: {
      colors: {
        accent: "#DC2626",
        secondary: "#3B82F6",
      },
    },
  },
};

export default config;
